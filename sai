#!/bin/bash

DESTDIR='/mnt'
FS='/tmp/sai_fs'
FSTAB='/tmp/sai_fstab'
LOG='/tmp/sai_log'

source /sai/sai_lib
source /sai/sai_config

sai_start() {
	say "sai will help you install Arch Linux"
}

sai_prepare() {
	say $FUNCNAME
	# cfdisk
	availdisks
	ask "pick a disk to partition?"
	cfdisk "/dev/$ans"
	
	# mountpount
	availdisks
	ask "select a partition for swap? (type 'no' if dont need one) "
	if [ "$ans" != "no" ]; then
		echo "$ans swap swap" >> $FS
	fi
	ask "select a partition for '/'? "
	echo "$ans / ext4" >> $FS
	
	ask "select a partition for '/boot'? (type 'no' if dont need one) "
	if [ "$ans" != "no" ]; then
		echo "$ans /boot ext2" >> $FS
	fi
	
	ask "sai asks you to use 'vim' to double-check everything. Hit 'enter' to continue."
	vim $FS
	
	
	for line in $(cat $FS); do
		PART=$(echo $line | cut -d ' ' -f 1)
		MP=$(echo $line | cut -d ' ' -f 2)
		FSTYPE=$(echo $line | cut -d ' ' -f 3)
		formatdisk $PART $MP $FSTYPE
		sleep 1
	done
}

sai_installpkgs() {
	say $FUNCNAME
	ask "sai asks you to edit the packages you want to install. Hit 'enter' to continue."
	vim /sai/packages.list
	pacman --root $DESTDIR --noconfirm -Sy "syslinux $(grep -v ^# /sai/packages.list)"
}

sai_postconfig() {
	say $FUNCNAME
	sai_chroot mkinitcpio -p linux
	sort $FSTAB >> $DESTDIR/etc/fstab
}

sai_bootloader() {
	say $FUNCNAME
	sai_chroot /usr/sbin/syslinux-install_update -iam
	ask "sai asks you to edit the bootloader menu. Hit 'enter' to continue."
	vim $DESTDIR/boot/syslinux/syslinux.cfg
}

sai_end() {
	say "sai did its job! you may 'reboot'"
}


sai_start
sai_prepare
sai_installpkgs
sai_config
sai_postconfig
sai_bootloader
sai_end
